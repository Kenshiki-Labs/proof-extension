// Plasmo's generated ID for the MAIN-world content script (see
// src/contents/page-observer.ts). Shared between background.ts (which
// verifies registration) and the e2e spec (which waits for it) so the two
// can't silently drift if Plasmo's naming scheme ever changes.
export const MAIN_WORLD_SCRIPT_ID = "srcContentsPageObserver"
